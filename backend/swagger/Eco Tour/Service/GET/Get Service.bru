meta {
  name: Get Service
  type: http
  seq: 1
}

get {
  url: http://localhost:8000/api/service/
  body: none
  auth: inherit
}

params:query {
  ~search: Ica
  ~reference_price_min: 100
  ~reference_price_max: 200
  ~type: private
  ~duration_min: 1080
  ~duration_max: 1081
  ~tags: CaMinata
}

settings {
  encodeUrl: true
  timeout: 0
}
